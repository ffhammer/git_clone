{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build GVC",
            "type": "shell",
            "command": "go",
            "args": [
                "build",
                "-o",
                "${env:HOME}/Desktop/gvc"
            ],
            "group": "build",
            "problemMatcher": []
        }
    ]
}